window.i18n.registerModuleTranslations('custom_json', 'de-DE', {
    // Translate keys if needed, but since it relies mostly on custom labels:
    'json_data_unavailable': 'Daten nicht verfügbar'
});
