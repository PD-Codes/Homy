[widget]
; type must match WIDGET['type'] in status.py and the key in WidgetRegistry.register().
type = example_status
name = Example: API Status
; Comma-separated list of integration ids that can power this widget.
; Must match the 'id' field in the integration's info.cfg.
integration_types = example_integration
icon = activity
default_size_x = 4
default_size_y = 3
